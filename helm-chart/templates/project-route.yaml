apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: deployment-test-route
  namespace: {{ .Values.namespace }}
spec:
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  to:
    kind: Service
    name: deployment-test-service
  port:
    targetPort: 8080